import { createStitches } from '@stitches/react'

export const {
    config,
    createTheme,
    css,
    globalCss,
    getCssText,
    keyframes,
    theme,
    styled,
} = createStitches({
    theme: {
        colors: {
            rocketseat: '#8257e6',
        }
    }
})

